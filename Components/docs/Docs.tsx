// file to create the component for the docs page

// importing the required files for the page
import React from "react";
import Navbar from "../navbar/Navbar";

const Docs = () => {
  return (
    <div>
      <div>
        <Navbar />
      </div>
      <>hei this is the docs section stay tuned for the latest updates</>
    </div>
  );
};

export default Docs;
